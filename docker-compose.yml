version: "3.9"

services:
  backend:
    build: ./backend
    container_name: travelmemory_backend
    ports:
      - "3001:3001"
    environment:
 #     - MONGO_URI=mongodb+srv://kavy_db_user:kavy@mongodb-kavy-aws.zj6khp2.mongodb.net/
      - MONGO_URI=mongodb://mongo:27017/
      - PORT=3001
    depends_on:
      - database
    networks:
      - travelmemory_network

  frontend:
    build: ./frontend
    container_name: travelmemory_frontend 
    ports:
      - "3000:3000"
    environment:
      - REACT_APP_BACKEND_URL=http://192.168.64.5:3001
    depends_on:
      - backend
    networks:
      - travelmemory_network
  
  database:
    image: mongo
    container_name: mongo
    ports:
      - "27017:27017"
    volumes:
      - mongo-data:/data/db
    networks:
      - travelmemory_network

volumes:
  mongo-data:

networks:
  travelmemory_network:
    driver: bridge